// Capacitor config lives at apps/terminal/capacitor.config.ts so `cap sync`
// discovers native plugins from apps/terminal/package.json dependencies.
export { default } from '../../capacitor.config';
