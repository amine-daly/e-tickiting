# Copy to keystore.properties and fill in your passwords.
# Do not commit keystore.properties or *.keystore files.

storeFile=terminal-production.keystore
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=terminal-production-key
keyPassword=YOUR_KEY_PASSWORD
