spring.application.name=eticketing-backend
server.port=${BACKEND_PORT:8080}

spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/eticketing}

springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
spring.data.mongodb.auto-index-creation=true

logging.level.org.springframework.security=INFO

# Ensure LocalDate is serialized as a string, not an array
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false

# Outbound mail (configure via environment variables when enabling ticket emails)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=587
spring.mail.username=aminedali44@gmail.com
spring.mail.password=bpgq eqjb vglv zdmf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
app.mail.from=aminedali44@gmail.com
app.mail.provider=${MAIL_PROVIDER:smtp}
app.mail.resend.apiKey=${RESEND_API_KEY:}
app.mail.resend.baseUrl=${RESEND_BASE_URL:https://api.resend.com}
