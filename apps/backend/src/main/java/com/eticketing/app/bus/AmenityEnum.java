package com.eticketing.app.bus;

/**
 * Amenity offered on a bus. Frontend maps each value to an icon and label.
 * Never use free text — always use this enum.
 */
public enum AmenityEnum {
    WIFI,
    POWER_OUTLET,
    TV,
    SNACKS,
    AC,
    TOILET,
    LUGGAGE,
    USB
}
